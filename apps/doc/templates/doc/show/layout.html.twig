{% extends 'base.html.twig' %}

{% block seo_title %}{{ page.meta.title }} - HarmonyUI for Symfony{% endblock %}

{% block seo_description -%}
    {{ page.meta.description ?: 'HarmonyUI documentation: accessible, extensible Twig components for Symfony, styled with Tailwind CSS.' }}
{%- endblock %}

{% block og_title %}{{ page.meta.title }}{% endblock %}

{% block body %}
    <div class="mx-auto flex w-full max-w-352 gap-12 px-6 py-8">
        <aside class="hidden w-56 shrink-0 lg:block">
            <nav class="sticky top-22 space-y-6">
                {% for category, pages in navigation %}
                    <div>
                        <p class="mb-2 text-xs font-semibold uppercase tracking-wide text-neutral-400 dark:text-neutral-500">{{ category }}</p>
                        <ul class="space-y-1 text-sm">
                            {% for item in pages %}
                                {% if item.slug == page.meta.slug %}
                                    {% set link_class = 'font-semibold text-neutral-900 dark:text-neutral-50' %}
                                {% elseif is_legacy_doc(item.slug) %}
                                    {% set link_class = 'text-neutral-300 hover:text-neutral-500 dark:text-neutral-700 dark:hover:text-neutral-500' %}
                                {% else %}
                                    {% set link_class = 'text-neutral-600 hover:text-neutral-900 dark:text-neutral-400 dark:hover:text-neutral-50' %}
                                {% endif %}
                                <li>
                                    <a href="{{ path('app_docs_show', {slug: item.slug}) }}" class="{{ link_class }}">
                                        {{ item.title }}
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endfor %}
            </nav>
        </aside>

        <article class="prose min-w-0 max-w-3xl flex-1">
            {% block content %}{% endblock %}
        </article>

        <aside class="hidden w-56 shrink-0 xl:block">
            {% if page.headings|length > 1 %}
                <nav class="sticky top-22" data-controller="toc" data-toc-offset-value="88">
                    <p class="mb-2 text-xs font-bold uppercase tracking-wide text-neutral-950 dark:text-neutral-50">On this page</p>
                    <ul class="text-sm">
                        {% for heading in page.headings %}
                            <li>
                                <twig:ui:Button
                                    as="a"
                                    variant="link"
                                    href="#{{ heading.id }}"
                                    data-toc-target="link"
                                    class="text-neutral-500 dark:text-neutral-400 font-normal whitespace-normal hover:text-neutral-950 dark:hover:text-neutral-50 data-active:text-neutral-950 dark:data-active:text-neutral-50 data-active:font-bold {{ heading.level == 3 ? 'pl-7' : heading.level == 2 ? 'pl-5' : 'pl-3' }}"
                                >
                                    {{ heading.text }}
                                </twig:ui:Button>
                            </li>
                        {% endfor %}
                    </ul>
                </nav>
            {% endif %}
        </aside>
    </div>
{% endblock %}
