{% set variant = inject('table_variant', 'default') %}
<tr
    class="{{ hui('table-row').apply({variant: variant}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'table-row',
    }) }}
>
    {%- block content %}{% endblock -%}
</tr>
