<tbody
    class="{{ hui('table-body').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'table-body',
    }) }}
>
    {%- block content %}{% endblock -%}
</tbody>
