<caption
    class="{{ hui('table-caption').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'table-caption',
    }) }}
>
    {%- block content %}{% endblock -%}
</caption>
