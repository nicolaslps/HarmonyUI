<thead
    class="{{ hui('table-header').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'table-header',
    }) }}
>
    {%- block content %}{% endblock -%}
</thead>
