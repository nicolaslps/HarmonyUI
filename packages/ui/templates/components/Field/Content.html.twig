<div
    class="{{ hui('field-content').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'field-content',
    }) }}
>
    {%- block content %}{% endblock -%}
</div>
