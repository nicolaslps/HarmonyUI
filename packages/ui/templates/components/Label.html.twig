<label
    class="{{ hui('label').apply({}, attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'label',
    }) }}
>
    {%- block content %}{% endblock -%}
</label>
